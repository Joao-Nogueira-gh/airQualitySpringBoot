Noida, India -bad
other - good

os 4 'bugs' encontrados eram == para strings em vez de equals

Replace this persistent entity with a simple POJO or DTO object.
ah e tal ok fixe mas idc

Make sure that command line arguments are used safely here.
Add Comment
foi retirado os args ja n eram usados
-----------
code smells

package airquality tem Q maisculo

Define a constant instead of duplicating this literal "very good" 6 times
sometimes fixed, sometimes not

2 println (replacye by logger)

return (secs>expiry) ? true : false ;
é igual a return (secs>expiry) ;

The return type of this method should be an interface such as "Map" rather than the implementation "HashMap"
The type of the "list" object should be an interface such as "List" rather than the implementation "ArrayList".
aceitava como arg arr list

Refactor this method to reduce its Cognitive Complexity from 17 to the 15 allowed.

Remove this empty statement. num if ;

Replace this if-then-else statement by a single return statement.
if exists
if (aqRepo.findByCityAndCountry(city,country) != null) {
            return true;
        }
        return false;
to
return aqRepo.findByCityAndCountry(city,country) != null;

Replace the synchronized class "StringBuffer" by an unsynchronized one such as "StringBuilder".
httpreq

Add at least one assertion to this test case.
autogen testclass

no fim so ficaram

o pojo/dto

pakcage name
method complexity
literal duplica

------------
jacoco
get, setters,+1's, evaluateforcss
enquanto q no controller é mainly a pag web, q foi avaliado com o selenium web driver
